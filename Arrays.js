var guestName = ["abhi" , "bhuvi" , "chini"]
var guestList = prompt("What is your name?"]
guestName.includes(guestList);

if (guestName.includes(guestList) {
  alert("Welcome");
} else {
  alert("Sorry,may be next time");
}
